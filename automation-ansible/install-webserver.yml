- hosts: Local
  connection: local
  tasks:

  - name: Creating a volume for webpage
    file:
      path: /root/webpage
      state: directory

  - name: Copy Content of webpage to dockerhos
    copy:
      dest: "/root/webpage"
      src: "index.html"
 
  - name: pull an image
    docker_image
      name: httpd
      source: pull

  - name: Launch container from httpd image
    docker_container:
      name: webos
      image: httpd
      state: started
      detach: yes
      interactive: yes
      ports:
        - "8080:443"
      volumes:
        - /root/wepage/:/usr/local/apache2/htdocs/
  